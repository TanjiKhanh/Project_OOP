package GameConditions;
/*  enum: special class that represents a group of constants
    condition PLAYING and MENU are defined */
public enum gameConditions {
    PLAYING, MENU, OPTIONS, QUIT;
    public static gameConditions condition = MENU;
}
